using UnityEngine;

public static class Constants
{
    /// <summary>
    /// to set values to zeroes when they are smaller than this
    /// </summary>
    public const float MIN_VALUE = 0.00001f;

    /// <summary>
    /// Width and height of a slot
    /// </summary>
    public const float SLOT_WIDTH = 150;
    /// <summary>
    /// Offset from the border of slot
    /// </summary>
    public const float SLOT_OFFSET = 20;
}
